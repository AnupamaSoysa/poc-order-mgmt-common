# poc-order-mgmt-common# poc-order-mgmt-common

Common utilities and shared logic for order management services.

## Purpose
This repository depends on upstream common models and provides shared
order-management-related functionality for downstream services.

## Dependencies
- **poc-common-model**  
  Version is tracked explicitly to ensure compatibility.

See `deps/common-model.version` for the referenced version.

## Notes
- This is a proof-of-concept (POC) repository
- Dependency versions are managed manually for clarity
